[package]
name = "rls-properties"
version = "0.1.0"
edition = "2021"
description = "Property-based tests for the RLS demo's security properties (proptest, differential)"

[dependencies]
proptest = "1"
reqwest = { version = "0.12", features = ["blocking", "json"] }
serde_json = "1"
base64 = "0.22"
